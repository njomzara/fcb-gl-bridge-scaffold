# GL Bridge Gap Register

## Baseline

- Architecture baseline: `C:\Users\Minja\Documents\FCBP GL Connector\FCBP_GL_Bridge_Transfer_Proposed_Solution_Architecture_v0.1_First_Draft.docx`
- DTS source folder: `C:\Users\Minja\Documents\FCBP GL Connector\DTS`
- Scaffold repository: `C:\Users\Minja\source\repos\fcbp-gl-bridge`
- Scan date: 2026-07-18

## Status Legend

- `Decision needed`: architecture or ownership decision must be made before final implementation.
- `DTS update`: detailed technical specification should be aligned or clarified.
- `Code update`: scaffold can be changed based on existing architecture/DTS intent.
- `External dependency`: blocked on released API, IAM, tenant, or platform details outside the scaffold.
- `Scaffold acceptable`: acceptable for now, but must not be mistaken for productive behavior.

## Gap Register

| ID | Priority | Area | Finding | Evidence | Required Decision / Action | Status |
| --- | --- | --- | --- | --- | --- | --- |
| GLT-GAP-001 | P1 | Outbox work vocabulary | `REPROCESS` previously existed in scaffold and some DTS text, while the architecture baseline work-type catalog lists `DISPATCH`, `RETRY`, `POLL`, `REBUILD`, and `STATUS_QUERY`. | Decision applied: `REPROCESS` is not a durable outbox work type. DTS text now treats it as operator action resolution, `/FCBP/IF_GLT_TYPES` no longer exposes `c_outbox_work_type-reprocess`, and the handler registry no longer maps `/FCBP/CL_GLT_WH_REPROCESS`. | Confirm final resolver policy inputs for choosing `STATUS_QUERY`, `RETRY`, `REBUILD`, or `DISPATCH`. | DTS/code aligned; policy details pending |
| GLT-GAP-002 | P0 | Package Builder / Outbox Dispatch | Repeated `DISPATCH` must reuse the current package when source and policy evidence are unchanged. | Decision applied: `/FCBP/IF_GLT_PACKAGE_REPO` exposes current-package lookup, productive/test repos implement it, and `/FCBP/CL_GLT_PACKAGE_PREPARER` checks current package consistency plus source and package-shaping policy hashes before creating a new package. | Final hardening remains tied to GLT-GAP-004 currentness locking/pointer ownership and GLT-GAP-011 durable hash implementation. | Code aligned; hardening pending |
| GLT-GAP-003 | P0 | Package Builder / Rebuild | `REBUILD` creates/validates a successor package and may enqueue controlled follow-up `DISPATCH` when explicitly approved. | Decision applied: Job Layer propagates `IMMEDIATE_DISPATCH` into outbox dispatch context, and `/FCBP/CL_GLT_WH_REBUILD` returns successor `DISPATCH` follow-up work after successful rebuild validation only when that flag is true for a real-time rebuild. Without approval, successor package remains `READY`. | Final policy hardening should bind `IMMEDIATE_DISPATCH` to tenant authorization/approval reference and active policy configuration. | Code/DTS aligned; policy hardening pending |
| GLT-GAP-004 | P0 | Package Repository / Currentness | Package publication must be concurrency-safe and maintain exactly one visible current package pointer per transfer. | Decision applied: `/FCBP/CL_GLT_PACKAGE_LOCK` claims `/FCBP/GLT_HDR-LOCK_OWNER/LOCK_UNTIL`; `/FCBP/CL_GLT_PACKAGE_REPO->PUBLISH_CURRENT` requires the lock owner, validates expected current/predecessor package, supersedes prior current package rows, marks the target package current, and updates `/FCBP/GLT_HDR-CURRENT_PACKAGE_ID` in the same LUW. Rebuild no longer owns current-pointer sync directly. | Final hardening remains around database-level uniqueness options and rollback semantics, tracked by GLT-GAP-005. | Code/DTS aligned; DB uniqueness hardening pending |
| GLT-GAP-005 | P0 | Package Repository / LUW | Package graph persistence inserts header, documents, lines, and source trace sequentially, but rollback/partial graph semantics are not explicit. | `/FCBP/CL_GLT_PACKAGE_REPO->persist_graph` inserts four table families. Handlers catch failures and mark work failed. | Define atomic LUW rule: either caller rollback is mandatory, or repository must mark/clean incomplete non-current evidence and consistency checks must block publication. | DTS update + code update |
| GLT-GAP-006 | P1 | Source Reading / Handoff | Productive source reading is not bound to released FCBP APIs/projections. | `/FCBP/CL_GLT_SRC_REPO_FCBP` fails closed; eligibility check is scaffold-permissive after shape validation. Source Reading DTS leaves released source API open. | Identify released FCBP projections/APIs for recon headers, accounting documents, lines, taxes, and source trace. Keep fail-closed until known. | External dependency |
| GLT-GAP-007 | P1 | Adapter | Productive adapter communication is not bound; only mock adapter path is runnable end to end. | Productive adapter client/classes fail closed with operation-not-bound behavior. Adapter DTS leaves exact target API/payload contract open. | Confirm S/4 Public/Private/CPI/on-prem adapter contracts, payload schema, idempotency key placement, and confirmation polling model. | External dependency |
| GLT-GAP-008 | P1 | Security / Authorization | Backend authorization service fails closed; DCL exists but runtime IAM/role action matrix is not configured. | `/FCBP/CL_GLT_AUTH_CHECK` rejects because authorization action is not configured. Audit/Security DTS marks role matrix and IAM binding open. | Define IAM roles, authorization object/claims mapping, support-ticket/reason policy, and action-specific checks. | External dependency |
| GLT-GAP-009 | P1 | Job / Ops | Generic Job Layer runner exists, but several `/FCBP/GLT_OPS` job wrappers are still TODO shells and duplicate responsibility boundaries. | `/FCBP/CL_GLT_JOB_RUNNER` dispatches work. Ops jobs for retry, poll confirm, status query, recon, retention, and registration recovery are not implemented. | Decide whether ops wrappers delegate to Job Layer runners/handlers or are removed from productive scope. Then implement delegation consistently. | Code update |
| GLT-GAP-010 | P1 | RAP / Operations UI | RAP behavior implementation classes are still TODO delegation shells. | `/FCBP/BP_GLT_TRANSFER`, `/FCBP/BP_GLT_TARGET_PROFILE`, and `/FCBP/BP_GLT_POLICY_ADMIN` contain TODO-level behavior. | Wire RAP actions to monitor/config/action services with authorization, reason capture, message mapping, and audit result handling. | Code update |
| GLT-GAP-011 | P1 | Evidence Hashing / Idempotency | Several hash/idempotency utilities use compact placeholder logic unsuitable for durable evidence. | Source hash, aggregation signature, config hash, request factory, and adapter evidence helpers contain placeholder hash/UUID logic. | Replace with ABAP Cloud-compatible stable digest/UUID implementation and canonical serialization rules. | Code update |
| GLT-GAP-012 | P2 | Audit Coverage | Operator-driven monitor actions write audit, but automated runtime transitions are not consistently backed by explicit audit records. | Monitor action service writes audit. Runtime handlers mostly write status/outbox evidence and messages. Audit DTS says runtime layers call Audit/Security at boundaries. | Decide whether status/outbox evidence is sufficient for automated transitions or whether handlers must also write audit events for retry, rebuild, submit, poll, and confirm. | DTS update |
| GLT-GAP-013 | P2 | Handoff Eligibility | Source eligibility check is permissive and not yet bound to productive source state rules. | `/FCBP/CL_GLT_SOURCE_ELIG_CHK` validates basic shape and otherwise permits. | Bind eligibility to final source status/event/version rules from released FCBP source APIs. | External dependency |
| GLT-GAP-014 | P2 | Test Framework | Happy-path test package exists, but productive blocked dependencies mean it exercises mock source/mock adapter rather than real released APIs. | `/FCBP/GLT_TEST` and `/FCBP/GLT_TEST_COCKPIT` provide seed/mock execution paths. | Keep tests as scaffold verification; add productive integration tests once source and adapter bindings are known. | Scaffold acceptable |
| GLT-GAP-015 | P2 | Tooling / Static Checks | `abaplint` invocation ignored the local config and emitted noisy default namespace/style findings. | `npx.cmd @abaplint/cli --config .abaplint.json` reported default config behavior and thousands of mostly naming/style issues. | Fix lint invocation/config discovery before using lint results as quality gates. | Code update |

## Recommended Work Sequence

1. Continue with the remaining architecture/DTS decisions: audit event expectations and package LUW/rollback semantics. `REPROCESS` vocabulary, current package reuse/publication ownership, and rebuild follow-up dispatch are resolved in the scaffold.
2. Patch the affected DTS sections so the detailed specs match the architecture baseline or explicitly propose architecture changes.
3. Re-run a code-vs-DTS gap pass using this register as the checklist.
4. Implement code updates in this order: package reuse/currentness, rebuild follow-up dispatch, package LUW/consistency rules, ops job delegation, RAP action delegation, stable hashing.
5. Keep source reading, adapter, and IAM behavior fail-closed until released API and authorization details are confirmed.
